Chapter 1: Intro

Python is an interpreted language: no need to compile and run an executible like Cpp.
In the background, code is compiled automatically into bytecode (assembly-style language).

No notion of main(). Any code outside of a function or class will be executed.

Can be run interactively via the command line unlike Cpp.
